{
  "name": "upload-artifact-cdn",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "axios": "^1.6.0",
    "fs": "0.0.1-security",
    "node-fetch": "^3.3.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
